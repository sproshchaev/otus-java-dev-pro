# kafka-schema-registry

```
kafka-schema-registry/
├── kafka-json-example/                   # Пример без Schema Registry (JSON)
│   ├── producer-service/                 # Сервис отправки сообщений в JSON
│   └── consumer-service/                 # Сервис получения сообщений в JSON
├── kafka-avro-example/                   # Пример с Schema Registry (Avro)
│   ├── producer-service/                 # Сервис отправки сообщений в Avro
│   └── consumer-service/                 # Сервис получения сообщений в Avro
├── docker-compose.yml                    # Контейнеры Kafka, Schema Registry
└── README.md
```
